<%= form_with(model: court_owner, local: true, class: "needs-validation") do |f| %>
    <% if court_owner.errors.any? %>
      <div class="alert alert-danger">
        <h4>Erros encontrados:</h4>
        <ul>
          <% court_owner.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="mb-3">
      <%= f.label :name, "Nome", class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
  
    <div class="mb-3">
      <%= f.label :email, "Email", class: "form-label" %>
      <%= f.email_field :email, class: "form-control" %>
    </div>
  
    <div class="mb-3">
      <%= f.label :phone_number, "Telefone", class: "form-label" %>
      <%= f.text_field :phone_number, class: "form-control" %>
    </div>
  
    <div class="mb-3">
      <%= f.label :address, "Endereço", class: "form-label" %>
      <%= f.text_field :address, class: "form-control" %>
    </div>
  
    <div class="mb-3">
      <%= f.label :password, "Senha", class: "form-label" %>
      <%= f.password_field :password, class: "form-control" %>
    </div>
  
    <div class="d-flex justify-content-between">
      <%= f.submit "Salvar", class: "btn btn-success" %>
    </div>
  <% end %>
  